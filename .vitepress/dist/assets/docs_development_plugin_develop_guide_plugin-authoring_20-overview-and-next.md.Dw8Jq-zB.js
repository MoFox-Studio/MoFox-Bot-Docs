import{_ as a,I as l,c as t,o as s,a4 as c,J as i}from"./chunks/framework.BR-X7tCu.js";const m=JSON.parse('{"title":"20. 先回头看一眼：你其实已经走到插件系统门口了","description":"","frontmatter":{},"headers":[],"relativePath":"docs/development/plugin_develop/guide/plugin-authoring/20-overview-and-next.md","filePath":"docs/development/plugin_develop/guide/plugin-authoring/20-overview-and-next.md","lastUpdated":1773412961000}'),d={name:"docs/development/plugin_develop/guide/plugin-authoring/20-overview-and-next.md"};function n(r,e,u,g,h,_){const o=l("NolebaseGitContributors"),p=l("NolebaseGitChangelog");return s(),t("div",null,[e[0]||(e[0]=c('<h1 id="_20-先回头看一眼-你其实已经走到插件系统门口了" tabindex="-1">20. 先回头看一眼：你其实已经走到插件系统门口了 <a class="header-anchor" href="#_20-先回头看一眼-你其实已经走到插件系统门口了" aria-label="Permalink to “20. 先回头看一眼：你其实已经走到插件系统门口了”">​</a></h1><blockquote><p><strong>导读</strong> 本章是一次回溯与收拢。前面十九章里，插件系统的各类组件和运行链路已经逐一铺开。这一章不引入新概念，而是把走过的路重新梳理一遍，并把下一阶段要进入的插件系统公共入口层——<code>plugin_system.base</code>、<code>plugin_system.api</code>、<code>plugin_system.types</code>——轻轻掀开一角，为后续打好衔接基础。</p></blockquote><p>这一段路其实已经走了很长。</p><p>如果你是跟着这份指南一路读下来的，那你现在看到的，已经不再是“怎么把一个插件勉强写出来”，而是插件系统里一整套比较完整的分层：</p><ul><li>插件是什么</li><li>组件是什么</li><li>配置怎么接</li><li>Tool、Agent、Action、EventHandler、Chatter、Router、Adapter 分别负责什么</li><li>消息又是怎么在线路里流动的</li></ul><p>很多时候，学习插件系统最难的地方不在某个单点 API，反而在于：</p><blockquote><p><strong>学着学着，脑子里东西越来越多，但一时说不清这些东西到底怎么拼在一起。</strong></p></blockquote><p>所以这一章不打算继续加新概念。</p><p>这一章更像一次回头看。</p><p>本章先把走过的路重新梳理一遍，再把下一阶段要进入的公共入口层轻轻掀开一角：</p><ul><li><code>plugin_system.base</code></li><li><code>plugin_system.api</code></li><li><code>plugin_system.types</code></li></ul><p>后面会专门讲它们。</p><p>这一章只做一件事：</p><blockquote><p><strong>让你知道，前面学到的那些分散概念，已经开始自然汇成“插件作者真正会使用的一套入口”。</strong></p></blockquote><h2 id="_20-1-最开始的时候-你学的是-插件能不能站起来" tabindex="-1">20.1 最开始的时候，你学的是“插件能不能站起来” <a class="header-anchor" href="#_20-1-最开始的时候-你学的是-插件能不能站起来" aria-label="Permalink to “20.1 最开始的时候，你学的是“插件能不能站起来””">​</a></h2><p>如果回忆最前面的几章，你最先接触的不是复杂组件，也不是统一消息模型。</p><p>你最先学的其实是更基础的东西：</p><ul><li>一个插件最少需要什么</li><li><code>manifest.json</code> 在做什么</li><li>插件类怎么注册</li><li>项目结构可以先多简单</li></ul><p>这一阶段的目标很朴素：</p><blockquote><p><strong>先让插件被系统识别、被系统加载。</strong></p></blockquote><p>这一步看起来好像很初级，但它其实很重要。</p><p>因为如果这一步没有站稳，后面所有“更高级”的内容都会变成空中楼阁。</p><p>所以现在回头看，最开始那几章真正帮你建立的，不是技巧，而是插件作者的第一个视角：</p><blockquote><p>“我写的不是随便一个 Python 文件，而是插件系统能识别的一种组件化扩展。”</p></blockquote><h2 id="_20-2-往前走一点-你学的是-插件内部可以长出哪些能力" tabindex="-1">20.2 往前走一点，你学的是“插件内部可以长出哪些能力” <a class="header-anchor" href="#_20-2-往前走一点-你学的是-插件内部可以长出哪些能力" aria-label="Permalink to “20.2 往前走一点，你学的是“插件内部可以长出哪些能力””">​</a></h2><p>再往后，视角就从“插件能不能加载”变成了：</p><blockquote><p><strong>插件加载之后，到底能以哪些方式参与系统。</strong></p></blockquote><p>这时候你接触到的是一组组件边界：</p><ul><li>Command</li><li>Service</li><li>Tool</li><li>Agent</li><li>Action</li><li>EventHandler</li><li>Chatter</li><li>Router</li><li>Adapter</li></ul><p>如果只看名字，这些东西一开始很容易像一串并列名词。</p><p>但你现在应该已经能慢慢分出来了：</p><ul><li>有些组件更像“能力点”</li><li>有些组件更像“流程控制点”</li><li>有些组件更像“系统入口”</li><li>有些组件更像“平台桥接层”</li></ul><p>这就是前面那一大段内容真正的价值。</p><p>它不是为了让你背组件列表，而是为了让你逐渐建立边界感。</p><p>而插件系统一旦有了边界感，很多问题就会突然变简单。</p><p>比如你会开始更自然地判断：</p><ul><li>“这个需求更像 Tool，不像 Agent。”</li><li>“这个需求应该放 Router，不该放 Command。”</li><li>“这个问题是 Adapter 层的，不是 Chatter 层的。”</li></ul><p>这其实就是一种很实在的进步。</p><h2 id="_20-3-再往后-你学到的是-组件不是孤立的-它们活在一条链路里" tabindex="-1">20.3 再往后，你学到的是“组件不是孤立的，它们活在一条链路里” <a class="header-anchor" href="#_20-3-再往后-你学到的是-组件不是孤立的-它们活在一条链路里" aria-label="Permalink to “20.3 再往后，你学到的是“组件不是孤立的，它们活在一条链路里””">​</a></h2><p>只理解组件还不够。</p><p>因为真实系统不是“很多组件摆在架子上”，而是“很多组件在链路里相互衔接”。</p><p>前面几章里，你已经开始接触到这种链路意识了。</p><p>比如：</p><ul><li>Prompt 和 LLM 不是凭空工作的，它们会和 Tool、Agent、Chatter 连在一起</li><li>Action 不是自己漂浮着执行，它通常接着发送链路或平台动作</li><li>Adapter 不是独立存在，它接着统一消息模型和平台协议</li><li><code>MessageEnvelope</code> 不是某个单独模块的私有格式，而是跨层传递的线路格式</li></ul><p>这意味着你现在对插件系统的理解，其实已经从“单个组件说明书”慢慢走向了：</p><blockquote><p><strong>一套分层协作的运行系统。</strong></p></blockquote><p>这一步很关键。</p><p>因为只有当你意识到它是系统，不是零件堆，你后面再学公共 API 层时，才会知道那些 API 为什么值得存在。</p><h2 id="_20-4-这一阶段最容易出现的感觉-会写了很多东西-但入口开始变散" tabindex="-1">20.4 这一阶段最容易出现的感觉：会写了很多东西，但入口开始变散 <a class="header-anchor" href="#_20-4-这一阶段最容易出现的感觉-会写了很多东西-但入口开始变散" aria-label="Permalink to “20.4 这一阶段最容易出现的感觉：会写了很多东西，但入口开始变散”">​</a></h2><p>走到这里，很多插件作者会开始出现一种很典型的感觉：</p><ul><li>好像已经知道很多概念了</li><li>也知道不少组件该怎么写</li><li>但真到自己开始写插件时，导入路径、公共类型、系统接口会慢慢变得有点散</li></ul><p>比如你会开始遇到这些问题：</p><ul><li>我到底应该从哪里 import 这些公共基类？</li><li>发送消息、读配置、查聊天流、调 LLM，到底该从哪一层拿？</li><li>常用的消息类型、LLM 类型、组件类型，是不是每次都要下钻到 <code>core</code> 或 <code>kernel</code> 里找？</li></ul><p>如果你开始有这种感觉，不是因为你退步了。</p><p>恰恰相反，这通常说明你已经从“理解概念”开始走向“真的准备写插件了”。</p><p>而这，也正是后面要进入公共入口层的原因。</p><h2 id="_20-5-为什么接下来要开始讲-plugin-system-的公共入口" tabindex="-1">20.5 为什么接下来要开始讲 <code>plugin_system</code> 的公共入口 <a class="header-anchor" href="#_20-5-为什么接下来要开始讲-plugin-system-的公共入口" aria-label="Permalink to “20.5 为什么接下来要开始讲 plugin_system 的公共入口”">​</a></h2><p>从代码组织上看，Neo-MoFox 当然有更底层的实现层：</p><ul><li><code>src/core/...</code></li><li><code>src/kernel/...</code></li></ul><p>这些层当然很重要。</p><p>但如果你是插件作者，其实并不希望自己每次写插件时都不停地下钻这些底层路径。</p><p>更理想的状态通常是：</p><blockquote><p><strong>插件作者有一层相对稳定、相对友好的公共入口，可以拿到最常用的基类、API 和类型。</strong></p></blockquote><p>而当前项目里，这层入口已经逐渐被整理成了：</p><ul><li><code>plugin_system.base</code></li><li><code>plugin_system.api</code></li><li><code>plugin_system.types</code></li></ul><p>再往上一层，还有一个聚合入口：</p><ul><li><code>plugin_system</code></li></ul><p>也就是说，前面你学到的那些东西，并不是永远散落在整个代码库里。</p><p>它们正在被收束成插件作者更好用的一层外观。</p><p>这就是后面几章要进入的重点。</p><h2 id="_20-6-base、api、types-现在先只记角色-不记细节" tabindex="-1">20.6 <code>base</code>、<code>api</code>、<code>types</code>，现在先只记角色，不记细节 <a class="header-anchor" href="#_20-6-base、api、types-现在先只记角色-不记细节" aria-label="Permalink to “20.6 base、api、types，现在先只记角色，不记细节”">​</a></h2><p>这一章不打算提前展开用法。</p><p>但你可以先把这三层当成三个不同的入口角色。</p><h3 id="plugin-system-base" tabindex="-1"><code>plugin_system.base</code> <a class="header-anchor" href="#plugin-system-base" aria-label="Permalink to “plugin_system.base”">​</a></h3><p>这一层你其实已经很熟了。</p><p>因为前面很多章节里，你一直在用的公共基类，本质上就在这里：</p><ul><li><code>BasePlugin</code></li><li><code>BaseTool</code></li><li><code>BaseAgent</code></li><li><code>BaseAction</code></li><li><code>BaseChatter</code></li><li><code>BaseRouter</code></li><li><code>BaseAdapter</code></li></ul><p>也就是说，<code>base</code> 更像是：</p><blockquote><p><strong>插件作者用来定义组件骨架的入口。</strong></p></blockquote><h3 id="plugin-system-api" tabindex="-1"><code>plugin_system.api</code> <a class="header-anchor" href="#plugin-system-api" aria-label="Permalink to “plugin_system.api”">​</a></h3><p>这一层更像是“运行中的系统能力入口”。</p><p>比如从名字上你已经能看出来它收拢的是哪些方向：</p><ul><li>聊天</li><li>发送</li><li>配置</li><li>Prompt</li><li>LLM</li><li>插件查询</li><li>路由</li><li>事件</li><li>存储</li></ul><p>也就是说，它回答的问题更接近：</p><blockquote><p><strong>插件写出来以后，运行时要怎样和系统能力交互。</strong></p></blockquote><h3 id="plugin-system-types" tabindex="-1"><code>plugin_system.types</code> <a class="header-anchor" href="#plugin-system-types" aria-label="Permalink to “plugin_system.types”">​</a></h3><p>这一层解决的则是另一类很实际的问题：</p><blockquote><p><strong>常用公共类型，到底从哪里拿。</strong></p></blockquote><p>它不是运行时 API，也不是组件基类。</p><p>它更像一层为插件作者准备的稳定类型出口，让你不用每次都下钻到更深层路径里去找：</p><ul><li><code>Message</code></li><li><code>MessageType</code></li><li><code>PromptTemplate</code></li><li><code>LLMPayload</code></li><li><code>ToolCall</code></li><li><code>ToolResult</code></li><li><code>TaskType</code></li></ul><p>这层看起来没那么“炫”，但写插件写多了以后，你会非常感谢它存在。</p><h2 id="_20-7-为什么这一步不是-补充知识-而是-真正进入插件作者视角" tabindex="-1">20.7 为什么这一步不是“补充知识”，而是“真正进入插件作者视角” <a class="header-anchor" href="#_20-7-为什么这一步不是-补充知识-而是-真正进入插件作者视角" aria-label="Permalink to “20.7 为什么这一步不是“补充知识”，而是“真正进入插件作者视角””">​</a></h2><p>这一点值得单独说明。</p><p>后面要讲的 <code>api</code> 和 <code>types</code>，不是那种“可学可不学的补充篇”。</p><p>它们更像是：</p><blockquote><p><strong>当你已经理解插件系统内部边界以后，终于开始从插件作者真正日常使用的入口来重新看整个系统。</strong></p></blockquote><p>前面你更像是在认识系统本体。</p><p>后面你会更像是在认识：</p><blockquote><p>“作为插件作者，我真正应该从哪一层接这个系统。”</p></blockquote><p>这两者其实不一样。</p><p>前者偏理解原理，后者偏形成稳定的编写习惯。</p><p>而一个插件作者一旦形成稳定习惯，后续写插件的速度和可维护性都会明显提高。</p><h2 id="_20-8-这也是为什么前面没有急着先讲-api-总表" tabindex="-1">20.8 这也是为什么前面没有急着先讲 API 总表 <a class="header-anchor" href="#_20-8-这也是为什么前面没有急着先讲-api-总表" aria-label="Permalink to “20.8 这也是为什么前面没有急着先讲 API 总表”">​</a></h2><p>你可能也会反过来问：</p><p>既然 <code>plugin_system.api</code> 和 <code>plugin_system.types</code> 这么重要，为什么不一开始就先讲？</p><p>答案其实很简单。</p><p>因为如果你在还没建立组件边界、消息模型、运行链路之前，就先看一堆 API 名字，体验往往会很差。</p><p>你会知道：</p><ul><li>有 <code>send_api</code></li><li>有 <code>chat_api</code></li><li>有 <code>prompt_api</code></li><li>有 <code>adapter_api</code></li></ul><p>但你不一定知道：</p><ul><li>这些 API 分别服务哪一层问题</li><li>它们和前面那些组件边界怎么对应</li><li>什么时候该用，什么时候不该用</li></ul><p>所以更合理的顺序其实正是现在这样：</p><ol><li>先理解插件系统里都有哪些角色和边界</li><li>再回头看插件作者的公共入口层</li></ol><p>这样你看到的就不再是“功能菜单”，而是一组有上下文的位置。</p><h2 id="_20-9-如果把前面的学习过程压成一条线-大概是这样" tabindex="-1">20.9 如果把前面的学习过程压成一条线，大概是这样 <a class="header-anchor" href="#_20-9-如果把前面的学习过程压成一条线-大概是这样" aria-label="Permalink to “20.9 如果把前面的学习过程压成一条线，大概是这样”">​</a></h2><p>你可以把前面一路走来的过程，粗略压成这样一条线：</p><ol><li>先理解插件是什么，怎样最小落地</li><li>再理解插件里能长出哪些组件</li><li>再理解这些组件分别卡在哪些系统边界上</li><li>再理解消息、平台、发送、对话这些链路怎么串起来</li><li>最后回到插件作者真正会碰到的公共入口层</li></ol><p>如果你现在正站在第 5 步门口，那其实说明前面的基础已经铺得差不多了。</p><p>后面要做的，不再是继续无止境地扩展概念，而是开始收拢使用入口。</p><h2 id="_20-10-这一章就先收在这里-下一阶段-开始从-组件世界-走向-入口世界" tabindex="-1">20.10 这一章就先收在这里：下一阶段，开始从“组件世界”走向“入口世界” <a class="header-anchor" href="#_20-10-这一章就先收在这里-下一阶段-开始从-组件世界-走向-入口世界" aria-label="Permalink to “20.10 这一章就先收在这里：下一阶段，开始从“组件世界”走向“入口世界””">​</a></h2><p>如果把这一章压成最后几句话，我最希望你记住的是：</p><ol><li>你前面学到的那些组件、链路和边界，并不是散的，它们已经开始自然汇合。</li><li>下一阶段不再只是继续认识组件，而是开始认识插件作者真正会使用的公共入口。</li><li><code>plugin_system.base</code>、<code>plugin_system.api</code>、<code>plugin_system.types</code>，就是这层入口的重要起点。</li></ol><p>接下来我们会开始专门讲这些入口。</p><p>但在进入细节之前，你现在先只需要保留一个很简单的感觉：</p><blockquote><p><strong>前面那一整段路，不是白走的，它正在把你带到“真正会写插件的人会怎样接系统”这一层。</strong></p></blockquote><p>下一章，一个很自然的起点就是：</p><blockquote><p><strong>先从 <code>plugin_system.api</code> 开始，看插件作者怎样通过公共 API 和系统运行时打交道。</strong></p></blockquote>',128)),i(o),i(p)])}const y=a(d,[["render",n]]);export{m as __pageData,y as default};
